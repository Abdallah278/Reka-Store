export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Authentication is Supabase Auth (see @/lib/supabase.ts). The previous Manus
// OAuth redirect flow was removed during the Supabase/Vercel migration.
